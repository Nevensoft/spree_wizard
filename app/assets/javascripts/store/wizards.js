//= require store/spree_frontend

//
// in order to have JS within a Rails application, you need to wrap the JS
//   within an anynomous function, which is then executed as the file is 
//   "rendered" to the page with the controller.
//

$(function(){


  

});